# Background video

The live background is stored once in WinterLock's Global Theme package.
`WinterFader.qml` resolves that bundled asset through Qt's standard data path;
this shell override intentionally contains no duplicate MP4.
